import 'package:flutter/material.dart';

class AppConstants {
  static final GlobalKey<NavigatorState> navigatorKey =
      GlobalKey<NavigatorState>();

  static String userKey = 'access_token';
  // static const String token = 'token';
}
